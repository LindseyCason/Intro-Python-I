Open up a file called bar which doesn't exist yet for writing. Write three lines of arbitrary content to that file, then close the file. Open up bartxt and inspect it to make sure that it contains what you expect it to contain